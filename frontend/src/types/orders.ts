
export type Order = {
    id: string;
    customerId: string;
    status: string;
    createdAt: string;
    updatedAt: string;
    items: [];
};

export type OrderItem = {
    id: string;
    customerId: string;
    status: string;
    createdAt: string;
    updatedAt: string;
    items: [];
};